<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Главная</title>
    <link rel="stylesheet" href="index.css">
</head>

<body>
    <div class="main-container">
        <nav class="navbar">
            <div class="navbar__block">
                <a href="/main" class="logo">
                    <img src="logo.jpg" alt="">
                </a>            

                <ul class="navbar__block__list">
                    <li><a href="/pledged_items" class="link">Заложенные вещи</a></li>
                    <li><a href="/contracts" class="link">Контракты</a></li>
                    <li><a href="/information" class="link">Информация</a></li>
                </ul>

                <a href="/exit" class="exit-link">Выйти</a>
            </div>
        </nav>

        <section class="section add-section">
            <table>
                <tr>
                    <td>ИД сотрудника</td>                       
                    <td>Фамилия</td>
                    <td>Имя</td>     
                    <td>ИД подразделения</td> 
                    <td>ИД должности</td>                                  
                    <td>Номер телефона</td>                                         
                </tr>

                <tr>
                    <form action="/change_employee" method="post">
                        <td><input type="number" name="employee_id" value="{{data.employee_id}}" readonly></td>                        
                        <td><input type="text" name="last_name" placeholder="Фамилия" value="{{data.last_name}}"></td>
                        <td><input type="text" name="first_name" placeholder="Имя" value="{{data.first_name}}"></td>
                        <td><select name="division_id" id="" aria-placeholder="ИД подразделения">
                            <option value="{{data.division_id}}" selected>{{data.division_id}}</option>
                            {{#each divisions}}
                                <option value="{{this.division_id}}">{{this.division_id}}</option>
                            {{/each}}
                        </select></td>
                        <td><select name="post_id" id="" aria-placeholder="ИД должности">
                            <option value="{{data.post_id}}" selected>{{data.post_id}}</option>
                            {{#each posts}}
                                <option value="{{this.post_id}}">{{this.post_id}}</option>
                            {{/each}}
                        </select></td>
                        <td><input type="tel" name="phone_number" placeholder="Номер телефона" value="{{data.phone_number}}"></td>                   
                        <button type="submit" class="add-btn">Сохранить</button>
                    </form>
                </tr>
            </table>
        </section>
    </div>
</body>